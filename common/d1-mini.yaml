esphome:
  name: ${DEVICENAME}
  platform: ESP8266
  board: d1_mini

wifi:
  ssid: !secret wifi_ssid
  password: !secret wifi_password
  
sensor:
  - platform: wifi_signal
    name: "${DEVICENAME} WiFi Signal"
    update_interval: 60s
  
status_led:
  pin:
    number: D4
    inverted: true